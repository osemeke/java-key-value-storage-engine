package main;

import java.util.LinkedList;

public class Compaction {

	public void run(LinkedList<Node> memtable) {

		// create all folders if not exist, based on config
		// convert list to hashmaps
		// store in sstable
		// create segments mergin from last folders up
		
		
	}

}
